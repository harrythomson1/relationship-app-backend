Connect locally to the postgres database with. The local postgres password is saved in .env as POSTGRES_PASSWORD
psql -h 127.0.0.1 -U postgres -d relationship_dev

For mocking mails we use mailhog. You can launch this by typing mailhog into the terminal
